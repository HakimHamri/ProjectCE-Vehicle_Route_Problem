# Vehicle Routing Problem (VRP) Solver using Evolution Strategy

This project implements a VRP solver using an Evolution Strategy (ES) algorithm.

## Features
- Handles multiple vehicles with capacity constraints
- Uses inversion mutation
- Visualizes convergence and routes

## Requirements
- Python
- numpy
- pandas
- matplotlib
